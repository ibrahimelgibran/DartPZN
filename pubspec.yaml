name: belajar_dart_pzn
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.2

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

name: my_dart_project
description: A new Dart project
environment:
  sdk: ">=2.0.0 <3.0.0" # Atur versi SDK Dart yang sesuai

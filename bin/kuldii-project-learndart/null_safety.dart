// operator baru ? ! late
// length = data panjangnya

void main() {
  // akan error yang disebebkan oleh null

  // String name: // tidak diperbolehkan datanya nnull
  // String? name: // diperbolehkan datanya nnull

  String name = "g";

  name = "datanya";

  // dipaksa & dianggap var nama ada datanya / tidak null
  // print (nama!.length);

  print(name.length);
}

/*
mendukung deklarasi data secara langsung saat pembuatan variable list,
mempermudah ketika membuat list jika memang datanya sudah siap.
 */
void main() {
  var names = <String>[
    'Ibrahim',
    'El',
    'Gibran',
  ];

  print(names);
  print(names.length);
}
